# Fodors-Zagats (FZ) rule templates loaded by the runner.
# Format: rule_id<TAB>description<TAB>expression
phone_exact	normalized phone numbers are identical	exact_phone
phone_area	phone area codes are identical	same_area
name_sim_ge_045	name token Jaccard similarity >= 0.45	name_sim >= 0.45
name_sim_ge_060	name token Jaccard similarity >= 0.60	name_sim >= 0.60
name_sim_ge_075	name token Jaccard similarity >= 0.75	name_sim >= 0.75
address_sim_ge_030	address token Jaccard similarity >= 0.30	address_sim >= 0.30
address_sim_ge_050	address token Jaccard similarity >= 0.50	address_sim >= 0.50
cuisine_sim_ge_050	cuisine token Jaccard similarity >= 0.50	cuisine_sim >= 0.50
same_city	city strings are identical after normalization	same_city
name045_address030	name similarity >= 0.45 AND address similarity >= 0.30	name_sim >= 0.45 and address_sim >= 0.30
name045_phone_area	name similarity >= 0.45 AND phone area codes match	name_sim >= 0.45 and same_area
phone_exact_or_name040_phone_area	phone exact OR (phone area matches AND name similarity >= 0.40)	exact_phone or (same_area and name_sim >= 0.40)
phone_exact_or_name035_phone_area	phone exact OR (phone area matches AND name similarity >= 0.35)	exact_phone or (same_area and name_sim >= 0.35)
address050_phone_area	address similarity >= 0.50 AND phone area codes match	address_sim >= 0.50 and same_area
name_or_cuisine_or_char060	name token overlap exists OR cuisine similarity >= 0.50 OR character-level name similarity >= 0.60	name_sim > 0 or cuisine_sim >= 0.50 or name_chars >= 0.60
address030_or_exact_name_or_name075	address similarity >= 0.30 OR normalized names are identical OR name similarity >= 0.75	address_sim >= 0.30 or exact_name or name_sim >= 0.75
phone_name_candidate_consistent	(phone exact OR phone area matches with name similarity >= 0.40) AND name/cuisine/character-name consistency AND address/name consistency	(exact_phone or (same_area and name_sim >= 0.40)) and (name_sim > 0 or cuisine_sim >= 0.50 or name_chars >= 0.60) and (address_sim >= 0.30 or exact_name or name_sim >= 0.75)
